import { Neo4jGraphqlInterceptor } from './neo4j-graphql-interceptor';

describe('Neo4jGraphqlInterceptor', () => {
  it('should be defined', () => {
    expect(new Neo4jGraphqlInterceptor()).toBeDefined();
  });
});
